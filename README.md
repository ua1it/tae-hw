# tae-hw
tae-hw example

example:
1번 solution 수정 및 각 README.md 파일 수정

```bash
# 현재까지 작업한 변경사항을 git에 추가한다.
git add .

# 추가한 변경사항들을 하나로 묶고 이름을 짓는다.
git commit -m "1번 풀이"

# 현재 커밋(이름을 짓고 묶여져있는) 되어있는 변경사항들을 실제 리모트(깃허브에 업로드 되어있는) 파일에 적용한다.
git push
```